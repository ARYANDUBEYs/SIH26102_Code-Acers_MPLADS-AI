# MPLAD Sentinel — Demo Backend Requirements
# Install with: pip install -r requirements.txt

# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# --- Database (SQLite for demo; SQLAlchemy makes swapping to Postgres trivial) ---
SQLAlchemy==2.0.52
pydantic==2.13.4

# --- Data processing ---
pandas==3.0.2
numpy==2.4.4

# --- Machine learning ---
scikit-learn==1.8.0
scipy==1.17.1

# --- Semantic similarity (duplicate/overlap detection) ---
# NOTE: sentence-transformers downloads the 'all-MiniLM-L6-v2' model (~90MB)
# from Hugging Face on first run, which requires internet access.
# If unavailable, ml/similarity.py automatically falls back to a TF-IDF
# based similarity approach so the demo still runs end-to-end offline.
sentence-transformers==6.0.0

# --- Contractor relationship / concentration analysis ---
networkx==3.6.1

# --- Image handling (kept available per project tech stack for future
# image-based verification features; not required by the current
# anomaly-detection pipeline) ---
opencv-python-headless==4.13.0.92
