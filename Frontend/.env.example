# Base URL of the FastAPI backend (Backend/). Defaults to localhost:8000 if unset.
# Set this to your deployed backend's URL when deploying the frontend (Render/Vercel/Netlify).
VITE_API_BASE=http://localhost:8000/api/v1
